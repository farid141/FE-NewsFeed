# FE NewsFeed

1. Instal semua library js dengan package manager seperti npm `npm install`
2. Buat file `.env` dari file `.example.env` sesuaikan port yang digunakan.
3. Jalankan development server `npm run dev`
